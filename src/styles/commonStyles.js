import { StyleSheet } from "react-native";

export const baseStyles = StyleSheet.create({
  flexCenter: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  textCenter: {
    textAlign: "center",
  },
});
